<?xml version="1.0" encoding="utf-8"?>
<!-- Debug builds only: lets a local build reach the Local API over
     http://localhost (via `adb reverse`). Release builds never include it. -->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application android:networkSecurityConfig="@xml/local_network_security_config" />
</manifest>
