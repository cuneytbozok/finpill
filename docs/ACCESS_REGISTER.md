# Access register

**Task:** 00.06 — Access register  
**Last reviewed:** 2026-09-24
**Secret policy:** This register contains ownership, status and evidence references only. It must never contain credentials, tokens, project URLs that disclose private resources, signing material, or unredacted fixture data.

## Operating rules

- `confirmed` means only the cited capability has evidence; it does not imply access to another environment or service.
- `unconfirmed` is a dependency gate, not authorization to create an account, configure a service, or use credentials.
- The project owner controls access grants and must update this register when an environment, owner, permitted use, or consuming task changes.
- Store secrets only in the approved service secret manager or local ignored environment file. Refer to a secret by service and environment, never by value.

## Register

| Prerequisite | Owner | Environment / identifier | Status | Rights or evidence | Consuming tasks / next action |
|---|---|---|---|---|---|
| GitHub repository | Project owner | `cuneytbozok/finpill`; `main` integration branch | confirmed | Repository and merged PRs #1–#4 are present in the configured `origin`; repository setup guide records it as public. | Ongoing. Maintain branch protection/required-check policy before relying on it for releases. |
| Supabase local workflow | Project owner | Disposable local `finpill-local` Supabase/Postgres instance | confirmed | Task 00.05 GitHub Actions replay passed; no hosted project or credentials are implied. | 01.06, 02.02 onward. Hosted staging/pilot projects, RLS and service ownership require separate evidence. |
| Supabase hosted project and Storage | Project owner | `gsgkoiwjkqbkuyyafzbf` designated by owner as task 01.06 staging; pilot project and Storage bucket not recorded | staging Data API and task 01.06 schema confirmed; pilot/Storage unconfirmed | Connected project listing showed active Finpill project; owner designated it staging. Foundation and profile/RLS migrations applied. Clerk development issuer is enabled as a third-party provider. Staging SQL role/claims probe passed ownership, reassignment, eligibility and anonymous checks; direct Data API returned 401 for anonymous/invalid bearer and 403 for an invited but pilot-ineligible Clerk session's insert. After explicit owner approval, that account's live Clerk session passed eligible Data API profile create/read/update (201/200/200) and ownership reassignment was denied (403). The dashboard labels its default branch `main PRODUCTION`; this does not establish a separate pilot environment. | 01.06 browser-issued API and two-live-account proof remain; 01.09 must establish staging/pilot isolation and release scopes. Storage remains gated for 02.03. |
| Clerk | Project owner | Development instance `ins_3JhfrCFuR2VJKzxOXqdjUbaZ1lw`; issuer `https://ample-chicken-233.clerk.accounts.dev` | development web and signed iOS lifecycle confirmed | Owner supplied the issuer and confirmed keys in ignored local files and Vercel. A read-only Backend API check returned this development instance. Dashboard Access mode showed Invite-only selected; an invited test account signed in and out through the local web shell, and its bearer request passed the separate protected API. On 2026-09-23 the owner showed the development Native API enabled, iOS app registered with App ID Prefix `7NA7D47449` and bundle ID `com.cuneytbozok.finpill`, and the native callback URL allowlisted. On a signed Release iPhone build, invited sign-in, token readiness, cold restart, offline failure/recovery, background resume, sign-out and cancellation passed. No credentials or personal details are recorded here. Staging and pilot instances remain unconfirmed. | 01.04–01.06 and 01.10. Establish pilot identity and approved deployment scopes before integrated proof; native bearer/API acceptance belongs to 01.10. |
| Clerk/Supabase token trust | Project owner | Development Clerk instance and owner-designated staging Supabase project above | configured for 01.06 staging | Owner approved enabling the Clerk Supabase integration and the Supabase Third-Party Auth connection for the development issuer. Dashboard showed both enabled; an active Clerk session token carried `role=authenticated` and reached the staging Data API. After an initial automatic approval rejection, the owner explicitly approved the account-level staging pilot grant and it succeeded. The token and account details were not printed or stored. | 01.06: prove browser-issued API application behavior and two-live-account isolation. Production-style identity remains 01.10. |
| Vercel client preview | Project owner | Existing Finpill client project; deployment URL is recorded in the 00.04 handoff | confirmed | PR #20's initial preview failed because two unused `NEXT_PUBLIC_SUPABASE_*` settings violated the static client's public-variable allowlist. With explicit owner approval, those two client-project settings were permanently deleted; API local configuration remains in ignored `.env.local`. Redeployed preview `39HrfrLDhfGp3zsL4aDtgYdMzE2F` reached Ready. The owner separately approved deletion of both `SUPABASE_SECRET_KEY` client-project entries (Preview and Development); Vercel confirmed removal. The secret itself remains valid at Supabase until rotated or revoked there. | 01.09. Confirm project ownership, staging/pilot isolation and separate API deployment. |
| Vercel task-branch client Preview | Project owner | `finpill` task-branch Preview `https://finpill-git-codex-0109-deploym-ef40e4-cuneyts-projects-ec980692.vercel.app` | Ready after `1d87017` | Chrome verified this static client and the separate `finpill-api` project deploy from the same repository into distinct projects. Only the exact task branch has public `NEXT_PUBLIC_API_ENABLED=true` and `NEXT_PUBLIC_API_ORIGIN` pointing to the API Preview. The new client Preview loaded in authenticated Chrome; Vercel Authentication still protects it from unauthenticated HTTP requests. The baked origin was not independently read from the built JavaScript. Hosted auth remains disabled, and Production client settings were not changed. | 01.09 acceptance disposition; 01.10 hosted signed bearer. |
| Vercel API deployment | Project owner | `finpill-api`, project `prj_RPrp1YttJigzDOBXIUI3cYGbBjJb`; task-branch Preview `https://finpill-api-git-codex-0109-dep-608ea4-cuneyts-projects-ec980692.vercel.app` | API Preview Ready and public v1 health verified | Chrome verified the project is connected to `cuneytbozok/finpill`, with Root Directory `apps/api`, outside-root files enabled, and workspace install/build commands. `APP_ENV=staging` and the exact task-branch client Preview origin are scoped to API Preview only. After the owner's explicit approval, only the exact task-branch Preview domain was exempted from Vercel Authentication; Production protection remains enabled. Public `/api/v1/health` returns `200` with `apiVersion=v1`; allowed and denied CORS preflights returned `204` and `403`, and a missing-bearer session returned `401`. API Preview has no Clerk/Supabase application credentials, so signed-bearer and pilot-isolation proof remain open. Production has no application variables or successful deployment. A manual branch build was classified Production and failed before API acceptance. | 01.09 acceptance disposition and 01.10 integrated auth. |
| KAP / MKK | Project owner | Development and production accounts/endpoints: not recorded | unconfirmed | The blueprint keeps production authentication TBD pending permitted account/documentation evidence; no credentials or protocol validation exists. | 03.01–03.07. Confirm account owner, environment, permitted endpoint, rate plan and retention terms before implementation. |
| Market-data provider | Project owner | Provider and environment: not selected | unconfirmed | No provider, historical coverage evidence, license, or pilot storage/display rights have been selected. | 02.08. Evaluate candidates and record the decision and rights evidence before an adapter is built. |
| AI provider / Vercel AI Gateway | Project owner | Provider/model and account: not selected | unconfirmed | The blueprint names an integration approach, not an account, model, credential, or usage approval. | AI-consuming tasks. Select under the relevant task and add owner, environment and permitted use. |
| Domains and deep-link hosts | Project owner | Provisional link host `finpill.vercel.app`; official domain not selected. Deployment origins for client/API environments remain separate prerequisites. | public association files confirmed; OS association deferred post-MVP | Owner accepted merged task 01.07 on 2026-09-24. Both public association endpoints returned `200` JSON with bytes matching the merged files; no iOS/Android OS link opening was observed. Personal Team cannot sign the iOS Associated Domains entitlement; local Android signing is not a distribution identity. The owner plans an official domain and Apple Developer Program enrollment after roadmap completion. | 01.09 must still establish client/API deployment origins and environment isolation. Official link domain, paid Apple signing and OS-verified Universal/App Links are post-MVP for the current private-use path. |
| iOS local identity and signing | Project owner | Bundle ID `com.cuneytbozok.finpill`; Personal Team `7NA7D47449` | signed local Release device build confirmed | Task 01.04's Xcode inspection initially found automatic signing with no team. The owner selected Personal Team `7NA7D47449` for Debug and Release in the task worktree; Codex did not edit signing settings. Xcode provisioned the owner's iPhone 15 Pro, and a signed Release build installed and launched. The app signature confirmed the team and bundle application identifier. This is local Personal Team signing, not store distribution. | 01.04 complete acceptance; review distribution signing only if a later release requires it. |
| Android local identity and signing | Project owner | Application ID `com.cuneytbozok.finpill`; local development/debug signing | signed Release emulator lifecycle confirmed | Task 01.02 verified the bundled shell. Task 01.05 built an auth-enabled Release APK signed with the local development keystore on the API 36 emulator. The owner privately signed in with an invited account; Codex observed token readiness, signed-in cold restart/process recreation, background/resume beyond one minute, Airplane Mode failure and recovery, sign-out, cancellation, and signed-out restart. No credential or token was recorded. The Clerk dashboard Android registration was not independently inspected, and distribution signing remains unselected. | 01.05 review/merge; production-style identity and API bearer acceptance remain 01.10. |
| Production signing and store ownership | Project owner | Apple Developer Program/App Store Connect, Google Play Console, production certificates and keystore custody: not selected | deferred | No App Store or Google Play publication is planned in the near term. Task 01.02 does not require store enrollment or production credentials for its local shell proof. | Reassess at 01.09/10.07 only if the chosen release/distribution method technically requires these assets; otherwise retain as a separate post-MVP store-publication gate. Never store signing secrets in the repository. |
| KAP financial-report fixtures | Project owner | Permitted source set and manifest: not selected | unconfirmed | No raw KAP payload or rights/provenance manifest is tracked. | 02.01, 03.01, 04.02–04.03. Select a diverse, permitted cohort and record retrieval time, hash, origin and usage rights. |

## Review triggers

Review this register before enabling an integration, adding a service secret, promoting a deployment, adding a fixture, or changing the responsible owner. A missing row blocks only its listed consuming tasks; it does not block unrelated roadmap work.
